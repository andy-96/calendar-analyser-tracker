{
  "scripts": {
    "start": "cd backend && yarn start",
    "build": "npm install -g yarn && cd frontend && yarn & yarn build && yarn stage && cd ../backend && yarn && yarn build"
  },
  "heroku-run-build-script": true
}
