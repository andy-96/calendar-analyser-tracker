{
  "name": "calendar-analyser-tracker",
  "engines": {
    "yarn": "1.x",
    "node": "14.x"
  },
  "scripts": {
    "start": "cd backend && node dist/main",
    "build": "cd frontend && yarn build && yarn stage && cd ../backend && yarn build",
    "deploy": "export NODE_ENV=production && cd backend && ~/Downloads/google-cloud-sdk/bin/gcloud app deploy && export NODE_ENV=development"
  }
}
