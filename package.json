{
  "scripts": {
    "start": "cd backend && yarn start",
    "build": "cd frontend && yarn & yarn build && yarn stage && cd ../backend && yarn && yarn build"
  },
  "heroku-run-build-script": true
}
