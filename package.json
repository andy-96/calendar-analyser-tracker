{
  "name": "calendar-analyser-tracker",
  "engines": {
    "yarn": "1.x",
    "node": "14.x"
  },
  "scripts": {
    "start": "cd backend && yarn start",
    "heroku-prebuild": "yarn global add @vue/cli && yarn add @vue/cli-service && cd frontend && yarn && cd ../backend && yarn",
    "build": "cd frontend && yarn build && yarn stage && cd ../backend && yarn build"
  },
  "heroku-run-build-script": true
}
